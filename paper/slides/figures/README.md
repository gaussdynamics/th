# Deck figure assets

Assets that belong to the deck rather than to the thesis or the notebooks.

- `ltd_stage07_prefix_brake_bug.png` — the Stage 7 figure **as published before
  the 2026-08-17 brake-direction fix**, showing the lead speed passing through
  zero at ~88 s and continuing to −100 km/h: a consist reversing at 99 km/h
  under full braking. Recovered from git commit `7bce5cd`
  (`notebooks/notebook_images/ltd_stage07_brake_buildup_power_limited_traction_states.png`).
  Used on the before/after slide in Act IV. The corrected version is the current
  file at that same path.
