# Core library (routegen) — minimal, pure-Python:
numpy>=1.24
requests>=2.28

# Interactive UI (app/streamlit_app.py):
streamlit>=1.30
pydeck>=0.8
pandas>=2.0
folium>=0.15          # clickable map for route selection
streamlit-folium>=0.20
